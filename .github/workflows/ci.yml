name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: [self-hosted, brainsait]
    steps:
      - uses: actions/checkout@v4
      - name: Run a test
        run: echo "Hello from brainsait runner"
